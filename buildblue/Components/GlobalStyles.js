import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
  white: {
    color: 'white'
  },
  debug: {
    borderColor: 'red',
    borderWidth: 5
  }
});

export default globalStyles;